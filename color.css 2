:root {
	--color-bg-deep: #183153;
	--color-primary: #204080;
	--color-surface: #f7f9fc;
	--color-text: #10202a;
	--color-muted: #6b7a8a;
	--color-gold: #d9a900; 
	--color-green: #2e7d32;
	--color-border: #dbe6f2;
}

@media (prefers-reduced-motion: reduce) {
	* { transition: none !important; animation: none !important; }
}

body {
	font-family: 'Segoe UI', Arial, sans-serif;
	margin: 0;
	/* white at the top, flowing down into the deep blue for a trustworthy look */
	background: linear-gradient(to bottom, #ffffff 0%, var(--color-surface) 20%, var(--color-bg-deep) 100%);
	color: var(--color-text);
}

.wrapper {
	max-width: 1100px;
	margin: 0 auto;
	padding: 0 20px;
	background: rgba(247,249,252,0.95);
	box-shadow: 0 2px 16px rgba(24,49,83,0.07);
	border-radius: 20px 20px 20px 20px;
}

.main-header {
	border: none;
	margin: 20px 0 0 0;
	padding: 18px 32px;
	display: flex;
	align-items: center;
	justify-content: space-between;
	background: #204080;
	color: #fff;
	border-radius: 8px 8px 0 0;
	box-shadow: 0 2px 8px rgba(24,49,83,0.08);
}
.header-content {
	width: 100%;
	display: flex;
	align-items: center;
	justify-content: space-between;
}
.logo {
	font-size: 2em;
	font-weight: 600;
	color: var(--color-surface);
	text-align: center
}
.tagline {
	font-size: 1em;
	margin-left: 20px;
	color: #e6eef8;
}
.login-btn {
	font-size: 1em;
	padding: 7px 24px;
	border: 2px solid var(--color-gold);
	background: #fff;
	color: var(--color-primary);
	border-radius: 4px;
	font-weight: 500;
	transition: background 0.15s ease, color 0.15s ease;
}
.login-btn:focus,
.login-btn:hover {
	background: var(--color-gold);
	color: var(--color-primary);
	outline: 3px solid rgba(255,215,0,0.18);
	outline-offset: 2px;
}
/* Navigation: UL-based */
.main-nav {
    /* reset previous simple block styles if present */
    background: #fff;
    margin: 0;
    padding: 8px 0;
    border-bottom: 2px solid var(--color-border);
    border-radius: 0 0 8px 8px;
}
.main-nav ul {
    display: flex;
    gap: 28px;
    margin: 0;
    padding: 6px 20px;
    list-style: none;
    align-items: center;
}
.main-nav li { margin: 0; }
.main-nav a {
    text-decoration: none;
    color: var(--color-primary);
    padding: 8px 10px;
    font-weight: 600;
    border-radius: 6px;
    transition: background 0.12s, color 0.12s;
}
.main-nav a:focus,
.main-nav a:hover {
    color: var(--color-gold);
    background: rgba(217,169,0,0.06);
    outline: none;
}

.hero {
	background: #eaf2fb;
	border: none;
	margin: 24px 0;
	padding: 36px 32px;
	display: flex;
	justify-content: space-between;
	align-items: center;
	border-radius: 8px;
	box-shadow: 0 2px 8px rgba(24,49,83,0.06);
}
.hero-text {
	max-width: 50%;
	text-align: center;
}
.hero-text h1 {
	font-size: 2.5em;
	font-weight: 700;
	margin-bottom: 10px;
	color: var(--color-primary);
	text-align: center;
}
.hero-text p {
	font-size: 1.125em;
	margin-bottom: 30px;
	color: var(--color-text);
}
.explore-btn {
	font-size: 1.125em;
	padding: 12px 36px;
	border: none;
	background: var(--color-gold);
	color: var(--color-primary);
	font-weight: 700;
	border-radius: 6px;
	cursor: pointer;
	box-shadow: 0 1px 6px rgba(16,32,42,0.08);
	transition: transform 0.06s ease, box-shadow 0.12s ease;
}
.explore-btn:focus,
.explore-btn:hover {
	background: var(--color-primary);
	color: var(--color-gold);
	transform: translateY(-1px);
	outline: 3px solid rgba(32,64,128,0.12);
	outline-offset: 3px;
}
.hero-image {
	border: 2px solid var(--color-border);
	width: 320px;
	height: 180px;
	display: flex;
	align-items: center;
	justify-content: center;
	background: #fff;
	border-radius: 8px;
}
.hero-image img {
	max-width: 90%;
	max-height: 90%;
}
.highlights {
	background: #fff;
	border: none;
	margin: 24px 0;
	padding: 24px 0;
	display: flex;
	gap: 30px;
	justify-content: space-between;
	border-radius: 8px;
	box-shadow: 0 2px 8px rgba(24,49,83,0.05);
}
.highlight-card {
	border: 1.5px solid #e0e6f0;
	padding: 24px 18px;
	width: 30%;
	display: flex;
	flex-direction: column;
	align-items: center;
	background: #f7f9fc;
	border-radius: 8px;
	box-shadow: 0 1px 4px rgba(24,49,83,0.04);
}
.highlight-card h2 {
	font-size: 1.15em;
	font-weight: 700;
	margin-bottom: 12px;
	text-align: center;
	color: var(--color-primary);
}
.highlight-card hr {
	width: 80%;
	margin: 10px 0;
	border-color: #e0e6f0;
}
.highlight-card button {
	font-size: 1em;
	padding: 10px 28px;
	border: none;
	background: var(--color-green);
	color: #fff;
	font-weight: 600;
	border-radius: 6px;
	cursor: pointer;
	margin-top: 20px;
	box-shadow: 0 1px 4px rgba(16,32,42,0.06);
	transition: background 0.15s ease;
}
.highlight-card button:hover,
.highlight-card button:focus {
	background: #216e2b;
	outline: 3px solid rgba(46,125,50,0.12);
	outline-offset: 2px;
}
.teaser-form {
	background: #eaf2fb;
	border: none;
	margin: 24px 0;
	padding: 24px 0;
	display: flex;
	gap: 30px;
	justify-content: space-between;
	border-radius: 8px;
	box-shadow: 0 2px 8px rgba(24,49,83,0.05);
}
.table-teaser, .cata-form {
	border: 1.5px solid #e0e6f0;
	padding: 24px 18px;
	width: 45%;
	background: #fff;
	border-radius: 8px;
	box-shadow: 0 1px 4px rgba(24,49,83,0.04);
}
.table-teaser h2, .cata-form h2 {
	font-size: 1.2em;
	font-weight: bold;
	margin-bottom: 10px;
	color: #204080;
}
.table-teaser hr, .cata-form hr {
	width: 80%;
	margin: 10px 0;
	border-color: #e0e6f0;
}
.table-teaser a {
	display: block;
	margin-top: 20px;
	color: var(--color-primary);
	text-decoration: underline;
	font-size: 1em;
	font-weight: 500;
	transition: color 0.12s;
}
.table-teaser a:focus,
.table-teaser a:hover {
	color: var(--color-gold);
	outline: 3px solid rgba(255,215,0,0.12);
	outline-offset: 2px;
}
.cata-form button {
	font-size: 1em;
	padding: 10px 28px;
	border: none;
	background: var(--color-gold);
	color: var(--color-primary);
	font-weight: 700;
	border-radius: 6px;
	cursor: pointer;
	margin-top: 20px;
	box-shadow: 0 1px 4px rgba(16,32,42,0.06);
	transition: background 0.12s ease, color 0.12s ease;
}
.cata-form button:focus,
.cata-form button:hover {
	background: var(--color-primary);
	color: var(--color-gold);
	outline: 3px solid rgba(32,64,128,0.12);
	outline-offset: 2px;
}
.main-footer {
	background: var(--color-primary);
	color: #fff;
	border: none;
	margin: 24px 0;
	padding: 16px 32px;
	display: flex;
	flex-direction: column;
	gap: 10px;
	font-size: 1em;
	border-radius: 0 0 8px 8px;
	box-shadow: 0 2px 8px rgba(16,32,42,0.08);
}
.footer-meta {
	display: flex;
	align-items: center;
	gap: 20px;
	margin-top: 10px;
}


a:focus, button:focus, input:focus {
	outline: 3px solid rgba(32,64,128,0.12);
	outline-offset: 3px;
}

/* skip link */
.skip-link {
	position: absolute;
	left: -999px;
	top: auto;
	width: 1px;
	height: 1px;
	overflow: hidden;
}
.skip-link:focus {
	position: static;
	width: auto;
	height: auto;
	padding: 8px 12px;
	margin: 8px 20px;
	background: var(--color-gold);
	color: var(--color-primary);
	border-radius: 4px;
	z-index: 9999;
}

/* Responsive: mobile-first adjustments */
@media (max-width: 768px) {
	.wrapper { padding: 0 12px; }
	.main-header { padding: 14px 18px; }
	.logo { font-size: 1.5rem; }
	.main-nav { gap: 16px; padding: 10px 6px; overflow-x: auto; -webkit-overflow-scrolling: touch; }
	.hero { flex-direction: column; padding: 20px; gap: 18px; }
	.hero-text { max-width: 100%; }
	.hero-text h1 { font-size: 1.6rem; }
	.hero-text p { font-size: 1rem; }
	.hero-image { width: 100%; height: auto; padding: 12px; }
	.hero-image img { width: 100%; height: auto; }
	.highlights { flex-direction: column; padding: 12px 0; }
	.highlight-card { width: 100%; margin-bottom: 12px; }
	.teaser-form { flex-direction: column; padding: 12px 0; }
	.table-teaser, .cata-form { width: 100%; margin-bottom: 12px; }
	.explore-btn, .cata-form button, .highlight-card button { width: auto; padding-left: 18px; padding-right: 18px; }
	.main-footer { padding: 12px 18px; }
}

@media (min-width: 769px) and (max-width: 1100px) {
	.hero-text h1 { font-size: 2rem; }
}
